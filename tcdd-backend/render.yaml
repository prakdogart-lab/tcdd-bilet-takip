services:
  - type: web
    name: tcdd-bilet-takip
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FCM_SERVER_KEY
        sync: false
    plan: free
